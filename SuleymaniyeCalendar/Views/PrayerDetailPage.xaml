<?xml version="1.0" encoding="utf-8" ?>

<ContentPage
    x:Class="SuleymaniyeCalendar.Views.PrayerDetailPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:localization="clr-namespace:LocalizationResourceManager.Maui;assembly=LocalizationResourceManager.Maui"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:viewModels="clr-namespace:SuleymaniyeCalendar.ViewModels"
    x:DataType="viewModels:PrayerDetailViewModel"
    Background="{AppThemeBinding Light={StaticResource AppBackgroundBrushLight}, Dark={StaticResource AppBackgroundBrushDark}}"
    FlowDirection="{DynamicResource FlowDirection}">

    <Grid>
    <ScrollView x:DataType="viewModels:PrayerDetailViewModel" VerticalScrollBarVisibility="Never">
        <VerticalStackLayout
            Padding="{StaticResource PagePaddingMobile}"
            x:DataType="viewModels:PrayerDetailViewModel"
            Spacing="{StaticResource SpacingDefault}">
            
            <!--  Compact Prayer Title Card  -->
            <Border Style="{StaticResource iOS26Tile}">
                <VerticalStackLayout Spacing="12">
                    <Label
                        FontAttributes="Bold"
                        HorizontalTextAlignment="Center"
                        Style="{StaticResource HeadlineLargeStyle}"
                        TextColor="{AppThemeBinding Light={StaticResource Neutral99},
                                                    Dark={StaticResource Neutral10}}"
                        Text="{Binding Title}" />
                    <BoxView
                        HeightRequest="2"
                        HorizontalOptions="Center"
                        WidthRequest="90"
                        Color="{AppThemeBinding Light={StaticResource GoldPure},
                                                Dark={StaticResource GoldMedium}}" />
                </VerticalStackLayout>
            </Border>

            <!--  Compact Time & Enable Card  -->
            <Border Style="{StaticResource FrostGlassCardFrozen}">
                <Grid ColumnDefinitions="*,Auto" ColumnSpacing="6" RowDefinitions="Auto,Auto" RowSpacing="2">
                    <!-- Time Section -->
                    <VerticalStackLayout Grid.Column="0" Grid.Row="0" Spacing="4">
                        <Label
                            Style="{StaticResource BodyMediumStyle}"
                            Text="{localization:Translate Vakti}"
                            TextColor="{AppThemeBinding Light={StaticResource Neutral90}, Dark={StaticResource Neutral10}}" />
                        <Label
                            FontAttributes="Bold"
                            FontSize="{DynamicResource SubHeaderFontSize}"
                            Text="{Binding Time}"
                            TextColor="{AppThemeBinding Light={StaticResource GoldDeep}, Dark={StaticResource GoldMedium}}" />
                    </VerticalStackLayout>
                    
                    <!-- Enable Switch Section -->
                    <VerticalStackLayout Grid.Column="1" Grid.Row="0" HorizontalOptions="End" Spacing="4">
                        <Label
                            HorizontalTextAlignment="Center"
                            Style="{StaticResource BodySmallStyle}"
                            Text="{localization:Translate EtkinlestirSwitch}"
                            TextColor="{AppThemeBinding Light={StaticResource Neutral90}, Dark={StaticResource Neutral10}}" />
                        <Switch
                            HorizontalOptions="Center"
                            AutomationProperties.IsInAccessibleTree="True"
                            IsToggled="{Binding Enabled}"
                            OnColor="{AppThemeBinding Light={StaticResource GoldDeep},
                                                      Dark={StaticResource GoldMedium}}"
                            SemanticProperties.Description="{localization:Translate EtkinlestirSwitchAciklama}"
                            ThumbColor="{AppThemeBinding Light={StaticResource GoldHighlight},
                                                         Dark={StaticResource GoldLight}}" />
                    </VerticalStackLayout>
                </Grid>
            </Border>

            <!--  Advanced options (platform dependent)  -->
            <!--<Border IsVisible="{Binding ShowAdvancedOptions}" Style="{StaticResource Card}">
                <VerticalStackLayout Spacing="8">
                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="8">
                        <Switch
                            Grid.Column="0"
                            AutomationProperties.IsInAccessibleTree="True"
                            IsToggled="{Binding Vibration}"
                            SemanticProperties.Description="{localization:Translate TitresimSwitch}">
                            <Switch.Behaviors>
                                <toolkit:EventToCommandBehavior
                                    Command="{Binding VibrationCheckedChangedCommand}"
                                    CommandParameter="{Binding Vibration}"
                                    EventName="Toggled" />
                            </Switch.Behaviors>
                        </Switch>
                        <Label
                            Grid.Column="1"
                            Style="{StaticResource LabelStyle}"
                            Text="{localization:Translate TitresimSwitch}"
                            VerticalTextAlignment="Center" />
                    </Grid>
                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="8">
                        <Switch
                            Grid.Column="0"
                            AutomationProperties.IsInAccessibleTree="True"
                            IsToggled="{Binding Alarm}"
                            SemanticProperties.Description="{localization:Translate AlarmSwitch}">
                            <Switch.Behaviors>
                                <toolkit:EventToCommandBehavior
                                    Command="{Binding AlarmCheckedChangedCommand}"
                                    CommandParameter="{Binding Alarm}"
                                    EventName="Toggled" />
                            </Switch.Behaviors>
                        </Switch>
                        <Label
                            Grid.Column="1"
                            Style="{StaticResource LabelStyle}"
                            Text="{localization:Translate AlarmSwitch}"
                            VerticalTextAlignment="Center" />
                    </Grid>
                </VerticalStackLayout>
            </Border>-->

            <!--  Compact Notification Settings Card  -->
            <Border IsVisible="{Binding Enabled}" Style="{StaticResource FrostGlassCardCrystal}">
                <VerticalStackLayout x:DataType="viewModels:PrayerDetailViewModel" Spacing="16">
                    <!--  Notification Time Section  -->
                    <VerticalStackLayout x:DataType="viewModels:PrayerDetailViewModel" Spacing="12">
                        <VerticalStackLayout Spacing="4">
                            <Label Style="{StaticResource TitleMediumStyle}" Text="{localization:Translate BildirmeZamani}" TextColor="{AppThemeBinding Light={StaticResource Neutral90}, Dark={StaticResource Neutral10}}" />
                            <Label Style="{StaticResource BodyMediumStyle}" TextColor="{AppThemeBinding Light={StaticResource Neutral90}, Dark={StaticResource Neutral10}}">
                                <Label.FormattedText>
                                    <FormattedString>
                                        <Span
                                            FontAttributes="Bold"
                                            FontSize="{DynamicResource DefaultFontSize}"
                                            Text="{Binding NotificationTime}" />
                                        <Span Text=" " />
                                        <Span Text="{localization:Translate DakikaOnceden}" />
                                    </FormattedString>
                                </Label.FormattedText>
                            </Label>
                        </VerticalStackLayout>
                        
                        <Border Style="{StaticResource GlassCardSoft}" Padding="12,8">
                            <Slider
                                AutomationProperties.IsInAccessibleTree="true"
                                HeightRequest="36"
                                HorizontalOptions="Fill"
                                Maximum="60"
                                MaximumTrackColor="{AppThemeBinding Light={StaticResource Neutral60},
                                                                    Dark={StaticResource Neutral60}}"
                                Minimum="0"
                                MinimumTrackColor="{AppThemeBinding Light={StaticResource GoldDeep},
                                                                    Dark={StaticResource GoldMedium}}"
                                SemanticProperties.Description="{localization:Translate BildirmeZamani}"
                                ThumbColor="{AppThemeBinding Light={StaticResource GoldPure},
                                                             Dark={StaticResource GoldMedium}}"
                                Value="{Binding NotificationTime}" />
                        </Border>
                    </VerticalStackLayout>

                    <!--  Sound Selection Section  -->
                    <VerticalStackLayout Spacing="12">
                        <Label Style="{StaticResource TitleMediumStyle}" Text="{localization:Translate Birsessecin}" TextColor="{AppThemeBinding Light={StaticResource Neutral99}, Dark={StaticResource Neutral10}}" />
                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
                            <Border Grid.Column="0" Style="{StaticResource GlassCardSoft}" Padding="12,8">
                                <Picker
                                    Title="{localization:Translate Birsessecin}"
                                    BackgroundColor="Transparent"
                                    HorizontalOptions="Fill"
                                    ItemsSource="{Binding AvailableSounds}"
                                    SelectedItem="{Binding SelectedSound}"
                                    TextColor="{AppThemeBinding Light={StaticResource OnSurfaceColor},
                                                                Dark={StaticResource OnSurfaceColorDark}}"
                                    FontSize="{DynamicResource DefaultFontSize}" />
                            </Border>
                                <Button Grid.Column="1"
                                    x:DataType="viewModels:PrayerDetailViewModel"
                                    Margin="6"
                                        Style="{StaticResource VistaAeroGlassButton}"
                                        TextColor="{AppThemeBinding Light={StaticResource OnSurfaceColor},
                                                                Dark={StaticResource OnSurfaceColorDark}}"
                                        Command="{Binding TestButtonClickedCommand}"
                                        WidthRequest="52" HeightRequest="48">
                                    <Button.Triggers>
                                        <DataTrigger Binding="{Binding IsPlaying}" TargetType="Button" Value="False">
                                            <Setter Property="ImageSource">
                                                <Setter.Value>
                                                    <FontImageSource FontFamily="{StaticResource IconFontFamily}"
                                                                     Glyph="&#xf04b;" Size="{DynamicResource IconSmallFontSize}" />
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsPlaying}" TargetType="Button" Value="True">
                                            <Setter Property="Style" Value="{StaticResource GlassButtonWarning}" />
                                            <Setter Property="ImageSource">
                                                <Setter.Value>
                                                    <FontImageSource FontFamily="{StaticResource IconFontFamily}"
                                                                     Glyph="&#xf04d;" Size="{DynamicResource IconSmallFontSize}" />
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>
                                    </Button.Triggers>
                                </Button>
                        </Grid>
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </Border>

            <!--  Close Button - Glass primary variant  -->
            <Button
                Style="{StaticResource GlassButtonSecondary}"
                Command="{Binding GoBackCommand}"
                Text="{localization:Translate Kapat}" />
        </VerticalStackLayout>
    </ScrollView>

    <!-- Scheduling overlay -->
    <Grid IsVisible="{Binding ShowOverlay}" BackgroundColor="#80000000">
        <Border Style="{StaticResource GlassCardSoft}" Padding="36,24" HorizontalOptions="Center" VerticalOptions="Center">
            <VerticalStackLayout Spacing="14" HorizontalOptions="Center">
                <ActivityIndicator IsRunning="True" Color="{StaticResource PrimaryColor}" WidthRequest="48" HeightRequest="48" />
                <Label Text="{Binding OverlayMessage}" Style="{StaticResource BodyLargeStyle}" HorizontalTextAlignment="Center" />
            </VerticalStackLayout>
        </Border>
    </Grid>
    </Grid>

</ContentPage>
