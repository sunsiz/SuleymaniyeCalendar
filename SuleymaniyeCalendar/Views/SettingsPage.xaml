<?xml version="1.0" encoding="utf-8" ?>

<ContentPage
    x:Class="SuleymaniyeCalendar.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:behaviors="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:localization="clr-namespace:LocalizationResourceManager.Maui;assembly=LocalizationResourceManager.Maui"
    xmlns:models="clr-namespace:SuleymaniyeCalendar.Models"
    xmlns:viewModels="clr-namespace:SuleymaniyeCalendar.ViewModels"
    x:DataType="viewModels:SettingsViewModel"
    FlowDirection="{DynamicResource FlowDirection}"
    Background="{AppThemeBinding Light={StaticResource AppBackgroundLight}, Dark={StaticResource AppBackgroundDark}}"
    Shell.BackgroundColor="{AppThemeBinding Light={StaticResource TabBarBgColor}, Dark={StaticResource TabBarBackgroundColorDark}}"
    Shell.ForegroundColor="{AppThemeBinding Light={StaticResource OnPrimaryColor}, Dark={StaticResource OnSurfaceColorDark}}"
    Shell.TitleColor="{AppThemeBinding Light={StaticResource OnPrimaryColor}, Dark={StaticResource OnSurfaceColorDark}}">

    <!-- Windows: explicit back navigation (Shell back button not always visible)
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="←" Priority="0" Order="Primary" Command="{Binding GoBackCommand}" />
    </ContentPage.ToolbarItems> -->

    <Shell.TitleView>
        <StackLayout Margin="8" HeightRequest="48" Orientation="Horizontal" FlowDirection="{DynamicResource FlowDirection}">
            <Label
                AutomationProperties.IsInAccessibleTree="True"
                SemanticProperties.HeadingLevel="Level1"
                Style="{StaticResource LabelSectionHeader}"
                Text="{localization:Translate UygulamaAyarlari}" />
        </StackLayout>
    </Shell.TitleView>

    <ContentPage.Content>
        <ScrollView
            x:DataType="viewModels:SettingsViewModel"
            HorizontalOptions="Fill"
            Orientation="Vertical"
            VerticalOptions="Fill"
            Padding="{StaticResource PagePaddingMobile}">
            <VerticalStackLayout x:DataType="viewModels:SettingsViewModel" Spacing="{StaticResource SpacingDefault}">

                <!--  Language Selection  -->
                <Border Style="{StaticResource SettingsCardOptimized}">
                    <Grid x:DataType="viewModels:SettingsViewModel" Padding="8,6" ColumnDefinitions="Auto,*,Auto" ColumnSpacing="8">
                        <!--  Golden language icon with increased visibility  -->
                        <Label
                            Grid.Column="0"
                            FontFamily="{StaticResource IconFontFamily}"
                            FontSize="{DynamicResource IconLargeFontSize}"
                            Text="&#xf0ac;"
                            TextColor="{AppThemeBinding Light={StaticResource CardValueLight}, Dark={StaticResource CardValueDark}}"
                            VerticalOptions="Center"
                            WidthRequest="44"
                            HorizontalTextAlignment="Center" />

                        <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Center">
                            <Label Style="{StaticResource HeadlineMediumStyle}" Text="{localization:Translate UygulamaDili}" />
                            <Label 
                                Style="{StaticResource BodySmallStyle}"
                                Text="{Binding SelectedLanguage.Name}"
                                Opacity="0.8" />
                        </VerticalStackLayout>

                        <Label
                            Grid.Column="2"
                            FontFamily="{StaticResource IconFontFamily}"
                            FontSize="{DynamicResource IconSmallFontSize}"
                            Text="&#xf054;"
                            TextColor="{AppThemeBinding Light={StaticResource OnSurfaceVariantLight}, Dark={StaticResource OnSurfaceVariantDark}}"
                            VerticalOptions="Center" />
                    </Grid>
                    
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnLanguageCardTapped" />
                    </Border.GestureRecognizers>
                </Border>

                <!-- Hidden Picker for Language Selection -->
                <Picker
                    x:Name="LanguagePicker"
                    Title="{localization:Translate UygulamaDiliniSecin}"
                    IsVisible="False"
                    ItemsSource="{Binding SupportedLanguages}"
                    SelectedItem="{Binding SelectedLanguage}" />

                <!--  Theme Selection  -->
                <Border Style="{StaticResource iOSLiquidGlassCard}">
                    <VerticalStackLayout x:DataType="viewModels:SettingsViewModel" Padding="8,6" Spacing="12">
                        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="8">
                            <!--  Golden theme icon with increased visibility  -->
                            <Label
                                Grid.Column="0"
                                FontFamily="{StaticResource IconFontFamily}"
                                FontSize="{DynamicResource IconLargeFontSize}"
                                Text="&#xf042;"
                                TextColor="{AppThemeBinding Light={StaticResource CardValueLight}, Dark={StaticResource CardValueDark}}"
                                VerticalOptions="Center"
                                WidthRequest="44"
                                HorizontalTextAlignment="Center" />
                            
                            <Label 
                                Grid.Column="1"
                                Style="{StaticResource HeadlineMediumStyle}" 
                                Text="{localization:Translate UygulamaTema}"
                                VerticalOptions="Center" />
                        </Grid>

                        <VerticalStackLayout x:DataType="viewModels:SettingsViewModel" Spacing="8">
                            <Border Style="{StaticResource Chip}"
                                    AutomationProperties.IsInAccessibleTree="True"
                                    SemanticProperties.Description="{localization:Translate AcikTema}">
                                <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="8">
                                    <Label Grid.Column="0" 
                                           FontFamily="{StaticResource IconFontFamily}" 
                                           FontSize="{DynamicResource IconMediumFontSize}" 
                                           Text="&#xf185;"
                                           TextColor="{AppThemeBinding Light={StaticResource HighContrastOnSurface}, Dark={StaticResource HighContrastOnSurfaceDark}}"
                                           VerticalOptions="Center" />
                                    <Label Grid.Column="1" 
                                           Text="{localization:Translate AcikTema}" 
                                           Style="{DynamicResource BodyMediumStyle}"
                                           TextColor="{AppThemeBinding Light={StaticResource HighContrastOnSurface}, Dark={StaticResource HighContrastOnSurfaceDark}}"
                                           FontSize="{DynamicResource BodyFontSize}"
                                           VerticalOptions="Center" />
                                    <RadioButton Grid.Column="2" GroupName="ThemeGroup" Value="1" IsChecked="{Binding LightChecked}" />
                                </Grid>
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer x:DataType="viewModels:SettingsViewModel" Command="{Binding SetThemeByIndexCommand}" CommandParameter="1" />
                                </Border.GestureRecognizers>
                                <Border.Triggers>
                                    <DataTrigger Binding="{Binding LightChecked}" Value="True" TargetType="Border">
                                        <Setter Property="Background" Value="{StaticResource Amber30}" />
                                    </DataTrigger>
                                </Border.Triggers>
                            </Border>
                            
                            <Border Style="{StaticResource Chip}"
                                    AutomationProperties.IsInAccessibleTree="True"
                                    SemanticProperties.Description="{localization:Translate KoyuTema}">
                                <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="8">
                                    <Label Grid.Column="0" 
                                           FontFamily="{StaticResource IconFontFamily}" 
                                           FontSize="{DynamicResource IconMediumFontSize}" 
                                           Text="&#xf186;"
                                           TextColor="{AppThemeBinding Light={StaticResource HighContrastOnSurface}, Dark={StaticResource HighContrastOnSurfaceDark}}"
                                           VerticalOptions="Center" />
                                    <Label Grid.Column="1" 
                                           Text="{localization:Translate KoyuTema}" 
                                           Style="{DynamicResource BodyMediumStyle}"
                                           TextColor="{AppThemeBinding Light={StaticResource HighContrastOnSurface}, Dark={StaticResource HighContrastOnSurfaceDark}}"
                                           FontSize="{DynamicResource BodyFontSize}"
                                           VerticalOptions="Center" />
                                    <RadioButton Grid.Column="2" GroupName="ThemeGroup" Value="0" IsChecked="{Binding DarkChecked}" />
                                </Grid>
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer x:DataType="viewModels:SettingsViewModel" Command="{Binding SetThemeByIndexCommand}" CommandParameter="0" />
                                </Border.GestureRecognizers>
                                <Border.Triggers>
                                    <DataTrigger Binding="{Binding DarkChecked}" Value="True" TargetType="Border">
                                        <Setter Property="Background" Value="{StaticResource Amber30}" />
                                    </DataTrigger>
                                </Border.Triggers>
                            </Border>
                            
                            <Border Style="{StaticResource Chip}"
                                    AutomationProperties.IsInAccessibleTree="True"
                                    SemanticProperties.Description="{localization:Translate SistemTemasi}">
                                <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="8">
                                    <Label Grid.Column="0" 
                                           FontFamily="{StaticResource IconFontFamily}" 
                                           FontSize="{DynamicResource IconMediumFontSize}" 
                                           Text="&#xf3ce;"
                                           TextColor="{AppThemeBinding Light={StaticResource HighContrastOnSurface}, Dark={StaticResource HighContrastOnSurfaceDark}}"
                                           VerticalOptions="Center" />
                                    <Label Grid.Column="1" 
                                           Text="{localization:Translate SistemTemasi}" 
                                           Style="{DynamicResource BodyMediumStyle}"
                                           TextColor="{AppThemeBinding Light={StaticResource HighContrastOnSurface}, Dark={StaticResource HighContrastOnSurfaceDark}}"
                                           FontSize="{DynamicResource BodyFontSize}"
                                           VerticalOptions="Center" />
                                    <RadioButton Grid.Column="2" GroupName="ThemeGroup" Value="2" IsChecked="{Binding SystemChecked}" />
                                </Grid>
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer x:DataType="viewModels:SettingsViewModel" Command="{Binding SetThemeByIndexCommand}" CommandParameter="2" />
                                </Border.GestureRecognizers>
                                <Border.Triggers>
                                    <DataTrigger Binding="{Binding SystemChecked}" Value="True" TargetType="Border">
                                        <Setter Property="Background" Value="{StaticResource Amber30}" />
                                    </DataTrigger>
                                </Border.Triggers>
                            </Border>
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </Border>

                <!--  Font Size  -->
                <Border Style="{StaticResource SettingsCardOptimized}" Padding="16,12">
                    <VerticalStackLayout x:DataType="viewModels:SettingsViewModel" Spacing="8">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="8">
                            <!--  Golden font size icon with increased visibility  -->
                            <Label
                                Grid.Column="0"
                                FontFamily="{StaticResource IconFontFamily}"
                                FontSize="{DynamicResource IconLargeFontSize}"
                                Text="&#xf031;"
                                TextColor="{AppThemeBinding Light={StaticResource CardValueLight}, Dark={StaticResource CardValueDark}}"
                                VerticalOptions="Center"
                                WidthRequest="44"
                                HorizontalTextAlignment="Center" />
                            
                            <Label 
                                Grid.Column="1"
                                Style="{StaticResource HeadlineMediumStyle}" 
                                Text="{localization:Translate YaziBoyutu}"
                                VerticalOptions="Center" />
                            
                            <!-- Font size badge -->
                            <Border 
                                Grid.Column="2"
                                Background="{StaticResource Amber30}"
                                Stroke="{AppThemeBinding Light={StaticResource Primary60}, Dark={StaticResource Primary40}}"
                                StrokeThickness="1.5"
                                StrokeShape="RoundRectangle 10"
                                Padding="8,4"
                                VerticalOptions="Center">
                                <Label
                                    Text="{Binding FontSize, StringFormat='{0:F0}'}"
                                    Style="{StaticResource LabelMediumStyle}"
                                    TextColor="{AppThemeBinding Light={StaticResource Primary50}, Dark={StaticResource Primary30}}"
                                    FontAttributes="Bold" />
                            </Border>
                        </Grid>
                        
                        <Slider
                            AutomationProperties.IsInAccessibleTree="true"
                            HeightRequest="44"
                            HorizontalOptions="Fill"
                            Margin="0,4,0,0"
                            Maximum="28"
                            Minimum="10"
                            SemanticProperties.Description="{localization:Translate YaziBoyutu}"
                            Style="{StaticResource PremiumGoldenSlider}"
                            Value="{Binding FontSize}" />
                    </VerticalStackLayout>
                </Border>

                <!--  Location Settings  -->
                <Border Style="{StaticResource iOSLiquidGlassCard}" Padding="14,10">
                    <Grid x:DataType="viewModels:SettingsViewModel" ColumnDefinitions="Auto,*,Auto" ColumnSpacing="8">
                        <!--  Golden location icon with increased visibility  -->
                        <Label
                            Grid.Column="0"
                            FontFamily="{StaticResource IconFontFamily}"
                            FontSize="{DynamicResource IconLargeFontSize}"
                            Text="&#xf3c5;"
                            TextColor="{AppThemeBinding Light={StaticResource CardValueLight}, Dark={StaticResource CardValueDark}}"
                            VerticalOptions="Center"
                            WidthRequest="44"
                            HorizontalTextAlignment="Center" />

                        <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Center">
                            <Label
                                Style="{StaticResource HeadlineMediumStyle}"
                                Text="{localization:Translate KonumYenileButton}" />
                            <Label
                                Style="{StaticResource BodySmallStyle}"
                                Text="{localization:Translate HepKonumYenile}"
                                Opacity="0.8" />
                        </VerticalStackLayout>
                        
                        <Switch
                            Grid.Column="2"
                            IsToggled="{Binding AlwaysRenewLocationEnabled}"
                            Style="{StaticResource GoldenSwitch}"
                            VerticalOptions="Center" />
                    </Grid>
                </Border>

                <!--  Android Notifications  -->
                <Border IsVisible="{OnPlatform Android=True, iOS=False}" Style="{StaticResource SettingsCardOptimized}" Padding="14,10">
                    <Grid x:DataType="viewModels:SettingsViewModel" ColumnDefinitions="Auto,*,Auto" ColumnSpacing="8">
                        <!--  Golden notification icon with increased visibility  -->
                        <Label
                            Grid.Column="0"
                            FontFamily="{StaticResource IconFontFamily}"
                            FontSize="{DynamicResource IconLargeFontSize}"
                            Text="&#xf0f3;"
                            TextColor="{AppThemeBinding Light={StaticResource CardValueLight}, Dark={StaticResource CardValueDark}}"
                            VerticalOptions="Center"
                            WidthRequest="44"
                            HorizontalTextAlignment="Center" />

                        <Label Grid.Column="1" Style="{StaticResource HeadlineMediumStyle}" Text="{localization:Translate BildirimNamazVakitleri}" VerticalOptions="Center" />
                        
                        <Switch
                            Grid.Column="2"
                            IsToggled="{Binding NotificationPrayerTimesEnabled}"
                            Style="{StaticResource GoldenSwitch}"
                            VerticalOptions="Center"
                            IsVisible="{Binding ShowNotificationPrayerOption}" />
                    </Grid>
                </Border>

                <!--  Android Foreground Service  -->
                <Border IsVisible="{OnPlatform Android=true, iOS=false}" Style="{StaticResource FrostGlassCardCrystal}" Padding="14,10">
                    <VerticalStackLayout x:DataType="viewModels:SettingsViewModel" Spacing="12">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="6">
                            <!--  Golden service icon with increased visibility  -->
                            <Label
                                Grid.Column="0"
                                FontFamily="{StaticResource IconFontFamily}"
                                FontSize="{DynamicResource IconLargeFontSize}"
                                Text="&#xf1e6;"
                                TextColor="{AppThemeBinding Light={StaticResource CardValueLight}, Dark={StaticResource CardValueDark}}"
                                VerticalOptions="Center"
                                WidthRequest="44"
                                HorizontalTextAlignment="Center" />

                            <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Center">
                                <Label
                                    Style="{StaticResource HeadlineMediumStyle}"
                                    Text="{localization:Translate BildirimKapatma}" />
                                <Label
                                    LineHeight="0.9"
                                    Style="{StaticResource BodySmallStyle}"
                                    Text="{localization:Translate BildirimUyari}"
                                    Opacity="0.8" />
                            </VerticalStackLayout>
                            
                            <Switch
                                Grid.Column="2"
                                IsToggled="{Binding ForegroundServiceEnabled}"
                                Style="{StaticResource GoldenSwitch}"
                                VerticalOptions="Center" />
                        </Grid>

                        <Button
                            Margin="8,0,8,10"
                            Style="{StaticResource GlassButtonPrimaryIntense}"
                            Command="{Binding GotoSettingsCommand}"
                            Text="{localization:Translate GotoSettings}"
                            TextColor="{AppThemeBinding Light={StaticResource OnSurfaceColor}, Dark={StaticResource OnSurfaceColorDark}}" />
                    </VerticalStackLayout>
                </Border>
            </VerticalStackLayout>
        </ScrollView>
    </ContentPage.Content>

</ContentPage>
