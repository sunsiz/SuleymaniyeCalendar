<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="SuleymaniyeCalendar.Views.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:localization="clr-namespace:LocalizationResourceManager.Maui;assembly=LocalizationResourceManager.Maui"
    xmlns:models="clr-namespace:SuleymaniyeCalendar.Models"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:viewModels="clr-namespace:SuleymaniyeCalendar.ViewModels"
    x:Name="MainPageRoot"
    Title="{Binding Title}"
    x:DataType="viewModels:MainViewModel"
    FlowDirection="{DynamicResource FlowDirection}"
    Background="{AppThemeBinding Light={StaticResource AppBackgroundBrushLight}, Dark={StaticResource AppBackgroundBrushDark}}">

    <Shell.TitleView>
        <Grid Margin="10" x:DataType="viewModels:MainViewModel" HeightRequest="40"
              ColumnDefinitions="*,Auto,Auto" ColumnSpacing="8">
            <Label Grid.Column="0"
                   AutomationProperties.IsInAccessibleTree="True"
                   FontAttributes="Bold"
                   FontSize="{DynamicResource HeaderFontSize}"
                   SemanticProperties.Description="{localization:Translate Takvim}"
                   SemanticProperties.HeadingLevel="Level1"
                   Style="{StaticResource LabelSectionHeader}"
                   Text="{localization:Translate PageTitle}"
                   VerticalTextAlignment="Start" />
            <Image Grid.Column="1"
                   x:DataType="viewModels:MainViewModel"
                   Margin="10,0,12,0"
                   AutomationProperties.IsInAccessibleTree="True"
                   VerticalOptions="Center"
                   SemanticProperties.Description="{localization:Translate UygulamaAyarlari}">
                <Image.Source>
                    <FontImageSource
                        FontAutoScalingEnabled="True"
                        FontFamily="{StaticResource IconFontFamily}"
                        Glyph="&#xf013;"
                        Size="{DynamicResource IconLargeFontSize}"
                        Color="{AppThemeBinding Light=White, Dark={StaticResource OnSurfaceColorDark}}" />
                </Image.Source>
                <Image.GestureRecognizers>
                    <TapGestureRecognizer x:DataType="viewModels:MainViewModel" Command="{Binding SettingsCommand}" NumberOfTapsRequired="1" />
                </Image.GestureRecognizers>
            </Image>
            <Image Grid.Column="2"
                   x:DataType="viewModels:MainViewModel"
                   Margin="10,0,20,0"
                   AutomationProperties.IsInAccessibleTree="True"
                   VerticalOptions="Center"
                   SemanticProperties.Description="{localization:Translate Yenile}">
                <Image.Source>
                    <FontImageSource
                        FontFamily="{StaticResource IconFontFamily}"
                        Glyph="&#xf2f1;"
                        Size="{DynamicResource IconLargeFontSize}"
                        Color="{AppThemeBinding Light=White, Dark={StaticResource OnSurfaceColorDark}}" />
                </Image.Source>
                <Image.GestureRecognizers>
                    <TapGestureRecognizer x:DataType="viewModels:MainViewModel" Command="{Binding RefreshLocationCommand}" NumberOfTapsRequired="1" />
                </Image.GestureRecognizers>
            </Image>
        </Grid>
    </Shell.TitleView>

    <ContentPage.Content>
        <RefreshView Command="{Binding RefreshLocationCommand}" IsRefreshing="{Binding IsRefreshing}">
            <Grid ColumnDefinitions="*" RowDefinitions="*" HorizontalOptions="Fill" Margin="0" Padding="0" >
                <CollectionView
                    x:Name="PrayersView"
                    HorizontalOptions="Center"
                    ItemsSource="{Binding Prayers}"
                    SelectedItem="{Binding SelectedPrayer, Mode=TwoWay}"
                    SelectionChangedCommand="{Binding PrayerSelectedCommand}"
                    SelectionMode="Single"
                    VerticalOptions="Fill"
                    VerticalScrollBarVisibility="Never">
                    <CollectionView.ItemsLayout>
                        <!-- 🎨 PHASE 16: ItemSpacing=0 - Let DataTriggers control ALL spacing -->
                        <LinearItemsLayout ItemSpacing="0" Orientation="Vertical" />
                    </CollectionView.ItemsLayout>
                    <CollectionView.Header>
                        <StackLayout x:DataType="viewModels:MainViewModel">
                            <!-- 🌙 PHASE 21: Clean Remaining Time Card with Animated Progress Gradient -->
                            <!-- 🌙 PHASE 21: Clean Remaining Time Card with Animated Progress -->
                            <Grid Margin="12,8" HorizontalOptions="Fill">
                                <!-- Base card -->
                                <Border
                                    Background="{AppThemeBinding Light={StaticResource BrandLightest}, Dark={StaticResource BrandDarker}}"
                                    Stroke="{AppThemeBinding Light={StaticResource BrandBase}, Dark={StaticResource BrandLight}}"
                                    StrokeThickness="2"
                                    StrokeShape="RoundRectangle 12"
                                    Shadow="{StaticResource ShadowFlat}">
                                    
                                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="8" Padding="12,10">
                                        <!-- Brown clock icon (brand color) -->
                                        <Border
                                            Grid.Column="0"
                                            WidthRequest="44"
                                            HeightRequest="44"
                                            Background="{AppThemeBinding Light={StaticResource SurfaceContainerLowestLight}, Dark={StaticResource SurfaceContainerDark}}"
                                            StrokeShape="RoundRectangle 22"
                                            VerticalOptions="Center">
                                            <Label
                                                FontFamily="{StaticResource IconFontFamily}"
                                                FontSize="24"
                                                Text="&#xf017;"
                                                TextColor="{AppThemeBinding Light={StaticResource PrimaryBase}, Dark={StaticResource PrimaryLight}}"
                                                VerticalOptions="Center"
                                                HorizontalTextAlignment="Center" />
                                        </Border>
                                        
                                        <Label
                                            Grid.Column="1"
                                            AutomationProperties.IsInAccessibleTree="True"
                                            FontSize="{DynamicResource SubHeaderFontSize}"
                                            FontAttributes="Bold"
                                            Text="{Binding RemainingTime}"
                                            TextColor="{AppThemeBinding Light={StaticResource OnSurfaceColor}, Dark={StaticResource OnSurfaceColorDark}}"
                                            VerticalOptions="Center" />
                                    </Grid>
                                </Border>
                                
                                <!-- 🎨 Animated Progress Bar at bottom - Shows time elapsed -->
                                <ProgressBar
                                    Progress="{Binding TimeProgress}"
                                    ProgressColor="{AppThemeBinding Light={StaticResource BrandBase}, Dark={StaticResource BrandLight}}"
                                    BackgroundColor="Transparent"
                                    VerticalOptions="End"
                                    HeightRequest="4"
                                    Margin="2,0,2,2" />
                            </Grid>
                        </StackLayout>
                    </CollectionView.Header>

                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="models:Prayer">
                            <ContentView Padding="0">
                                <!-- 🎨 PHASE 16: Pure DataTrigger margin system - NO ItemSpacing conflicts -->
                                <ContentView.Triggers>
                                    <!-- 🎨 PHASE 16: Ultra-compact past prayers - zero vertical, minimal horizontal -->
                                    <DataTrigger Binding="{Binding IsPast}" TargetType="ContentView" Value="True">
                                        <Setter Property="Margin" Value="8,0,8,0" />
                                    </DataTrigger>
                                    <!-- 🦸 PHASE 11: Generous margin for hero current prayer -->
                                    <DataTrigger Binding="{Binding IsActive}" TargetType="ContentView" Value="True">
                                        <Setter Property="Margin" Value="10,4,10,4" />
                                    </DataTrigger>
                                    <!-- Standard margin for upcoming prayers -->
                                    <MultiTrigger TargetType="ContentView">
                                        <MultiTrigger.Conditions>
                                            <BindingCondition Binding="{Binding IsActive}" Value="False" />
                                            <BindingCondition Binding="{Binding IsPast}" Value="False" />
                                        </MultiTrigger.Conditions>
                                        <Setter Property="Margin" Value="10,2,10,2" />
                                    </MultiTrigger>
                                </ContentView.Triggers>
                                <Grid>
                                <!-- 🎨 PHASE 21: Clean glass morphism prayer cards -->
                                <Border
                                    Background="{AppThemeBinding Light={StaticResource SurfaceGlassBrushLight}, Dark={StaticResource SurfaceGlassBrushDark}}"
                                    Stroke="{AppThemeBinding Light={StaticResource OutlineColorLight}, Dark={StaticResource OutlineColorDark}}"
                                    StrokeThickness="1"
                                    StrokeShape="RoundRectangle 12"
                                    Shadow="{StaticResource ShadowFlat}">
                                    <Border.Triggers>
                                        <!-- 🎨 PHASE 21: Past prayer - Reduced opacity, compact size -->
                                        <DataTrigger
                                            Binding="{Binding IsPast}"
                                            TargetType="Border"
                                            Value="True">
                                            <Setter Property="Opacity" Value="0.5" />
                                            <Setter Property="MinimumHeightRequest" Value="56" />
                                            <Setter Property="HeightRequest" Value="56" />
                                            <Setter Property="MaximumHeightRequest" Value="56" />
                                            <Setter Property="Padding" Value="8,4" />
                                        </DataTrigger>
                                        <!-- 🎨 PHASE 21: Current prayer - Brown left accent (brand color) -->
                                        <DataTrigger
                                            Binding="{Binding IsActive}"
                                            TargetType="Border"
                                            Value="True">
                                            <Setter Property="Background" Value="{AppThemeBinding Light={StaticResource BrandLight}, Dark={StaticResource BrandDarker}}" />
                                            <Setter Property="Stroke" Value="{AppThemeBinding Light={StaticResource PrimaryBase}, Dark={StaticResource PrimaryLight}}" />
                                            <Setter Property="StrokeThickness" Value="3" />
                                            <Setter Property="Shadow" Value="{StaticResource ShadowRaised}" />
                                            <Setter Property="HeightRequest" Value="120" />
                                            <Setter Property="Padding" Value="20,16" />
                                        </DataTrigger>
                                        <!-- 🎨 PHASE 21: Upcoming prayer - Standard glass card -->
                                        <MultiTrigger TargetType="Border">
                                            <MultiTrigger.Conditions>
                                                <BindingCondition Binding="{Binding IsActive}" Value="False" />
                                                <BindingCondition Binding="{Binding IsPast}" Value="False" />
                                            </MultiTrigger.Conditions>
                                            <Setter Property="Opacity" Value="0.95" />
                                        </MultiTrigger>
                                    </Border.Triggers>

                                    <Grid
                                        ColumnDefinitions="Auto,*,Auto,Auto"
                                        ColumnSpacing="8">

                                        <!-- Animated Prayer Icon -->
                                        <Border
                                            Grid.Column="0"
                                            WidthRequest="40"
                                            HeightRequest="40"
                                            Background="{AppThemeBinding Light={StaticResource BrandLight}, Dark={StaticResource SurfaceColorDark}}"
        									Stroke="{AppThemeBinding Light={StaticResource OutlineColorLight}, Dark={StaticResource OutlineColorDark}}"
                                            StrokeThickness="1"
                                            StrokeShape="RoundRectangle 20"
                                            VerticalOptions="Center">
                                            <Border.Triggers>
                                                <!-- 🎨 PHASE 16: Compact icon for past prayers -->
                                                <DataTrigger Binding="{Binding IsPast}" TargetType="Border" Value="True">
                                                    <Setter Property="WidthRequest" Value="28" />
                                                    <Setter Property="HeightRequest" Value="28" />
                                                    <Setter Property="Opacity" Value="0.7" />
                                                </DataTrigger>
                                                <!-- 🦸 PHASE 9 HERO: Enhanced golden glow for current prayer icon container -->
                                                <DataTrigger Binding="{Binding IsActive}" TargetType="Border" Value="True">
                                                    <Setter Property="Background" Value="{StaticResource GoldLight}" />
                                                    <Setter Property="Stroke" Value="{StaticResource GoldPure}" />
                                                    <Setter Property="StrokeThickness" Value="2.5" />
                                                    <Setter Property="WidthRequest" Value="52" />
                                                    <Setter Property="HeightRequest" Value="52" />
                                                    <Setter Property="Shadow">
                                                        <Shadow Brush="{StaticResource GoldOrange}" Radius="12" Offset="0,3" Opacity="0.4" />
                                                    </Setter>
                                                </DataTrigger>
                                            </Border.Triggers>
                                            <Image
                                                Source="{Binding IconPath}"
                                                Aspect="AspectFit"
                                                WidthRequest="32"
                                                HeightRequest="32"
                                                VerticalOptions="Center"
                                                HorizontalOptions="Center">
                                                <Image.Triggers>
                                                    <!-- 🎨 PHASE 16: Smaller icon for past prayers -->
                                                    <DataTrigger
                                                        Binding="{Binding IsPast}"
                                                        TargetType="Image"
                                                        Value="True">
                                                        <Setter Property="WidthRequest" Value="20" />
                                                        <Setter Property="HeightRequest" Value="20" />
                                                        <Setter Property="Opacity" Value="{AppThemeBinding Light=0.6, Dark=0.55}" />
                                                    </DataTrigger>
                                                    <!-- 🦸 PHASE 9 HERO: Enhanced icon scaling for current prayer -->
                                                    <DataTrigger
                                                        Binding="{Binding IsActive}"
                                                        TargetType="Image"
                                                        Value="True">
                                                        <Setter Property="Opacity" Value="1.0" />
                                                        <Setter Property="Scale" Value="1.35" />
                                                        <Setter Property="WidthRequest" Value="40" />
                                                        <Setter Property="HeightRequest" Value="40" />
                                                    </DataTrigger>
                                                    <!-- Upcoming prayers: High visibility in both modes -->
                                                    <MultiTrigger TargetType="Image">
                                                        <MultiTrigger.Conditions>
                                                            <BindingCondition Binding="{Binding IsActive}" Value="False" />
                                                            <BindingCondition Binding="{Binding IsPast}" Value="False" />
                                                        </MultiTrigger.Conditions>
                                                        <Setter Property="Opacity" Value="{AppThemeBinding Light=0.95, Dark=0.85}" />
                                                    </MultiTrigger>
                                                </Image.Triggers>
                                            </Image>
                                        </Border>

                                        <!-- Prayer Name with State-Based Colors -->
                                        <Label
                                            Grid.Column="1"
                                            Style="{StaticResource PrayerNameStyle}"
                                            Text="{Binding Name}"
                                            VerticalOptions="Center">
                                            <Label.Triggers>
                                                <!-- 🎨 PHASE 16: Compact text for past prayers -->
                                                <DataTrigger
                                                    Binding="{Binding IsPast}"
                                                    TargetType="Label"
                                                    Value="True">
                                                    <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource PrayerPastTextColor}, Dark={StaticResource PrayerPastTextColorDark}}" />
                                                    <Setter Property="FontSize" Value="{DynamicResource BodySmallFontSize}" />
                                                    <Setter Property="Opacity" Value="0.8" />
                                                </DataTrigger>
                                                <!-- 🦸 PHASE 11: Enhanced to TitleFontSize for maximum hero impact -->
                                                <DataTrigger
                                                    Binding="{Binding IsActive}"
                                                    TargetType="Label"
                                                    Value="True">
                                                    <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource PrayerActiveTextColor}, Dark={StaticResource PrayerActiveTextColorDark}}" />
                                                    <Setter Property="FontAttributes" Value="Bold" />
                                                    <Setter Property="FontSize" Value="{DynamicResource TitleFontSize}" />
                                                    <Setter Property="CharacterSpacing" Value="1.0" />
                                                </DataTrigger>
                                                <MultiTrigger TargetType="Label">
                                                    <MultiTrigger.Conditions>
                                                        <BindingCondition Binding="{Binding IsActive}" Value="False" />
                                                        <BindingCondition Binding="{Binding IsPast}" Value="False" />
                                                    </MultiTrigger.Conditions>
                                                    <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource PrayerUpcomingTextColor}, Dark={StaticResource PrayerUpcomingTextColorDark}}" />
                                                </MultiTrigger>
                                            </Label.Triggers>
                                        </Label>

                                        <!-- Prayer Time with State-Based Colors -->
                                        <Label
                                            Grid.Column="2"
                                            HorizontalTextAlignment="End"
                                            Style="{StaticResource PrayerTimeStyle}"
                                            Text="{Binding Time}"
                                            VerticalOptions="Center">
                                            <Label.Triggers>
                                                <!-- 🎨 PHASE 16: Compact text for past prayers -->
                                                <DataTrigger
                                                    Binding="{Binding IsPast}"
                                                    TargetType="Label"
                                                    Value="True">
                                                    <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource PrayerPastTextColor}, Dark={StaticResource PrayerPastTextColorDark}}" />
                                                    <Setter Property="FontSize" Value="{DynamicResource BodySmallFontSize}" />
                                                    <Setter Property="Opacity" Value="0.8" />
                                                </DataTrigger>
                                                <!-- 🦸 PHASE 11: Enhanced to TitleFontSize for maximum hero impact -->
                                                <DataTrigger
                                                    Binding="{Binding IsActive}"
                                                    TargetType="Label"
                                                    Value="True">
                                                    <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource PrayerActiveTextColor}, Dark={StaticResource PrayerActiveTextColorDark}}" />
                                                    <Setter Property="FontAttributes" Value="Bold" />
                                                    <Setter Property="FontSize" Value="{DynamicResource TitleFontSize}" />
                                                    <Setter Property="CharacterSpacing" Value="1.0" />
                                                </DataTrigger>
                                                <MultiTrigger TargetType="Label">
                                                    <MultiTrigger.Conditions>
                                                        <BindingCondition Binding="{Binding IsActive}" Value="False" />
                                                        <BindingCondition Binding="{Binding IsPast}" Value="False" />
                                                    </MultiTrigger.Conditions>
                                                    <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource PrayerUpcomingTextColor}, Dark={StaticResource PrayerUpcomingTextColorDark}}" />
                                                </MultiTrigger>
                                            </Label.Triggers>
                                        </Label>

                                        <!-- Enhanced Notification Bell with Clean Design -->
                                        <Border
                                            Grid.Column="3"
                                            Background="{AppThemeBinding Light={StaticResource SurfaceVariantColor}, Dark={StaticResource SurfaceColorDark}}"
        									Stroke="{AppThemeBinding Light={StaticResource OutlineColorLight}, Dark={StaticResource OutlineColorDark}}"
                                            StrokeThickness="1"
                                            StrokeShape="RoundRectangle 18"
                                            WidthRequest="36"
                                            HeightRequest="36"
                                            VerticalOptions="Center">
                                            <Label
                                                AutomationProperties.IsInAccessibleTree="True"
                                                FontFamily="{StaticResource IconFontFamily}"
                                                FontSize="{DynamicResource IconMediumFontSize}"
                                                HorizontalOptions="Center"
                                                VerticalOptions="Center"
                                                Text="&#xe7f6;"
                                                TextColor="{AppThemeBinding Light={StaticResource HighContrastOnSurface}, Dark={StaticResource HighContrastOnSurfaceDark}}">
                                                <Label.Triggers>
                                                    <DataTrigger
                                                        Binding="{Binding Enabled}"
                                                        TargetType="Label"
                                                        Value="False">
                                                        <Setter Property="Text" Value="&#xf1f6;" />
                                                        <Setter Property="SemanticProperties.Description" Value="{localization:Translate DevreDisi}" />
                                                        <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource HighContrastOutline}, Dark={StaticResource HighContrastOutlineDark}}" />
                                                        <Setter Property="Opacity" Value="0.6" />
                                                    </DataTrigger>
                                                    <DataTrigger
                                                        Binding="{Binding Enabled}"
                                                        TargetType="Label"
                                                        Value="True">
                                                        <Setter Property="Text" Value="&#xf0f3;" />
                                                        <Setter Property="SemanticProperties.Description" Value="{localization:Translate Etkin}" />
                                                        <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource Success60}, Dark={StaticResource Success30}}" />
                                                        <Setter Property="Opacity" Value="1.0" />
                                                    </DataTrigger>
                                                    <!-- 🌟 GOLDEN HOUR: Golden bell for enabled current prayer -->
                                                    <MultiTrigger TargetType="Label">
                                                        <MultiTrigger.Conditions>
                                                            <BindingCondition Binding="{Binding IsActive}" Value="True" />
                                                            <BindingCondition Binding="{Binding Enabled}" Value="True" />
                                                        </MultiTrigger.Conditions>
                                                        <Setter Property="TextColor" Value="{StaticResource GoldPure}" />
                                                        <Setter Property="Scale" Value="1.1" />
                                                    </MultiTrigger>
                                                </Label.Triggers>
                                            </Label>
                                            <Border.GestureRecognizers>
                                                <TapGestureRecognizer x:DataType="models:Prayer"
                                                    Command="{Binding NavigateCommand}" />
                                            </Border.GestureRecognizers>
                                            <Border.Triggers>
                                                <DataTrigger
                                                    Binding="{Binding Enabled}"
                                                    TargetType="Border"
                                                    Value="True">
                                                    <Setter Property="BackgroundColor" Value="{AppThemeBinding Light={StaticResource Success10}, Dark={StaticResource Success95}}" />
                                                    <Setter Property="Stroke" Value="{AppThemeBinding Light={StaticResource Success40}, Dark={StaticResource Success60}}" />
                                                    <Setter Property="StrokeThickness" Value="1.25" />
                                                </DataTrigger>
                                                <!-- 🌟 GOLDEN HOUR: Golden background for enabled current prayer bell -->
                                                <MultiTrigger TargetType="Border">
                                                    <MultiTrigger.Conditions>
                                                        <BindingCondition Binding="{Binding IsActive}" Value="True" />
                                                        <BindingCondition Binding="{Binding Enabled}" Value="True" />
                                                    </MultiTrigger.Conditions>
                                                    <Setter Property="Background" Value="{StaticResource GoldLight}" />
                                                    <Setter Property="Stroke" Value="{StaticResource GoldPure}" />
                                                    <Setter Property="StrokeThickness" Value="1.5" />
                                                </MultiTrigger>
                                                <DataTrigger
                                                    Binding="{Binding Enabled}"
                                                    TargetType="Border"
                                                    Value="False">
                                                    <Setter Property="Stroke" Value="{StaticResource GlassStrokeBrush}" />
                                                    <Setter Property="StrokeThickness" Value="0.75" />
                                                </DataTrigger>
                                            </Border.Triggers>
                                        </Border>
                                    </Grid>
                                </Border></Grid>
                            </ContentView>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>

                    <CollectionView.Footer>
                        <StackLayout x:DataType="viewModels:MainViewModel" Orientation="Vertical" Spacing="{StaticResource SpacingTight}" Margin="0,4,0,4">
                            <!--  Monthly Calendar Button - 8px grid spacing ✅  -->
                            <Button
                                Margin="32,0,32,0"
                                Style="{StaticResource GlassButtonPillSecondary}"
                                Text="{localization:Translate AylikTakvim}"
                                Command="{Binding GoToMonthCommand}"
                                HorizontalOptions="Fill"
                                IsEnabled="{Binding IsNotBusy}">
                                <Button.ImageSource>
                                    <FontImageSource
                                        FontFamily="{StaticResource IconFontFamily}"
                                        Glyph="&#xf073;"
                                        Size="{DynamicResource IconMediumSize}"
                                        Color="{AppThemeBinding Light={StaticResource Primary95}, Dark={StaticResource Primary10}}" />
                                </Button.ImageSource>
                            </Button>

                            <!--  City Location Card - 8px grid spacing ✅  -->
                            <Border
                                Margin="32,0,32,8"
                                HorizontalOptions="Fill"
                                IsEnabled="{Binding IsNotBusy}"
                                Style="{StaticResource LocationCard}">
                                <Label
                                    Margin="3,0"
                                    Padding="3"
                                    x:DataType="viewModels:MainViewModel"
                                    AutomationProperties.IsInAccessibleTree="True"
                                    HorizontalOptions="Center"
                                    SemanticProperties.Description="{localization:Translate Sehir}"
                                    Style="{StaticResource LocationStyle}"
                                    VerticalTextAlignment="Center">
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span
                                                FontFamily="{StaticResource IconFontFamily}"
                                                FontSize="{DynamicResource SubHeaderFontSize}"
                                                Text="&#xf3c5; " />
                                            <Span Text="{Binding City}" TextTransform="Uppercase" />
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding GoToMapCommand}" NumberOfTapsRequired="1" />
                                </Border.GestureRecognizers>
                            </Border>
                        </StackLayout>
                    </CollectionView.Footer>
                </CollectionView>

                <!-- 🎨 PHASE 15: Long running overlay with ElevatedPrimaryCard + GoldenActivityIndicator -->
                <Grid IsVisible="{Binding ShowOverlay}" BackgroundColor="#80000000">
                    <Border Style="{StaticResource ElevatedPrimaryCard}" Padding="32,20" HorizontalOptions="Center" VerticalOptions="Center">
                        <VerticalStackLayout Spacing="12" HorizontalOptions="Center">
                            <!-- 🎨 White activity indicator for visibility on golden card -->
                            <ActivityIndicator IsRunning="True" Color="White" WidthRequest="40" HeightRequest="40" />
                            <Label Text="{Binding OverlayMessage}" Style="{StaticResource BodyLargeStyle}" HorizontalTextAlignment="Center" />
                        </VerticalStackLayout>
                    </Border>
                </Grid>
            </Grid>
        </RefreshView>
    </ContentPage.Content>
</ContentPage>
